## 家庭成员智能体测试报告

日期：2026-04-17

目标测试文件：

- `test/test_family_member_agent_runtime.py`

执行命令：

```bash
python3 -m pytest test/test_family_member_agent_runtime.py -q
```

执行结果：

- 通过数量：2
- 失败数量：0
- 总体结果：通过
- 耗时：6.97 秒

---

## 一、这次测了什么

这份测试主要覆盖两个核心能力。

第一部分是“家庭成员信息智能体的创建与多轮信息采集流程”。

它验证了下面这条链路是否完整：

1. 用户先发起“创建家庭成员智能体”。
2. 系统进入创建流程，要求补充智能体用途。
3. 用户补充“主要记录家庭成员的信息”。
4. 系统进入确认阶段。
5. 用户完成创建。
6. 系统把智能体正式激活，并切换到家庭成员信息 schema。
7. 用户开始录入“爸爸”的成员信息。
8. 系统按字段顺序逐步追问。
9. 最终完成信息入库，并保存联系方式、邮箱和额外备注。

第二部分是“已保存知识的查询能力”。

它验证了：

1. 在同一个会话中先完成家庭成员信息录入。
2. 然后提问“爸爸的手机号是多少？”。
3. 系统能命中已保存知识。
4. 返回自然语言答案：`爸爸的手机号是1234567890。`

---

## 二、测试流程记录

### 1. 创建智能体阶段

测试先发送：

- `帮我创建家庭成员的智能体`

预期行为：

- 任务意图应被识别成 `create_information_agent`
- 系统进入 `awaiting_purpose`

实际结果：

- 符合预期。


### 2. 细化用途阶段

测试继续发送：

- `主要记录家庭成员的信息。`

预期行为：

- 任务意图应切换成 `refine_information_agent`
- 系统进入 `awaiting_confirmation`

实际结果：

- 符合预期。


### 3. 完成创建阶段

测试继续发送：

- `完成创建家庭成员信息智能体`

预期行为：

- 系统应生成并激活家庭成员信息智能体
- schema 中应包含 `member_name`
- 知识命名空间应为 `agent_knowledge/family_member_info_agent`

实际结果：

- 符合预期。


### 4. 成员信息采集阶段

测试随后发送：

- `将爸爸信息添加到家庭成员智能体中`

系统进入知识采集流程，按顺序追问：

1. 成员姓名
2. 角色
3. 称呼
4. 默认币种
5. 是否参与消费统计
6. 是否参与日程提醒
7. 特殊约束
8. 联系方式
9. 邮箱
10. 额外备注

测试里实际输入的数据是：

- 姓名：章三
- 角色：爸爸
- 称呼：老爸
- 默认币种：JPY
- 参与消费统计：是
- 参与日程提醒：是
- 特殊约束：无
- 手机号：1234567890
- 邮箱：abc@gmail.com
- 额外备注：公司、联系电话、Line 账号等补充说明

预期行为：

- 每一步都应切换到下一个字段
- 最后一步应进入 `knowledge_added`
- 保存的结构化知识里应包含手机号、邮箱和备注

实际结果：

- 全部符合预期。


### 5. 已保存知识查询阶段

在第二个测试中，先完整走完同样的创建和录入流程，然后发送：

- `爸爸的手机号是多少？`

预期行为：

- 任务意图识别为 `query_agent_knowledge`
- 返回自然语言答案
- 查询结果中存在知识命中

实际结果：

- 符合预期。
- 返回答案正确：`爸爸的手机号是1234567890。`

---

## 三、测试结论

当前 `test_family_member_agent_runtime.py` 已经可以稳定执行。

这说明目前下面几件事是通的：

1. 家庭成员信息智能体的创建流程是通的。
2. 多轮字段采集流程是通的。
3. 结构化知识保存是通的。
4. 基于已保存知识的手机号查询是通的。
5. 测试执行过程中生成的 trace 已经被隔离到临时 generated 目录，不会污染正式目录。

---

## 四、这次测试的价值

这份测试现在不仅是在测“能不能返回 200”，而是在测一条完整业务链：

- 创建智能体
- 激活智能体
- 多轮采集
- 保存知识
- 知识检索
- 自然语言回答

因此，它已经可以作为后续阶段测试中的一个稳定基线。

---

## 五、后续手动复测建议

如果后面改动了下面这些模块，建议优先手动重跑这份测试：

- `intent_analyzer`
- `task_decomposer`
- `workflow_planner`
- `execution_coordinator`
- `information_agent_service`
- `vector_store`

推荐命令：

```bash
python3 -m pytest test/test_family_member_agent_runtime.py -q
```

如果要顺便避免旧 trace 干扰其它回归，建议先跑：

```bash
./scripts/run_isolated_runtime_regression.sh
```
