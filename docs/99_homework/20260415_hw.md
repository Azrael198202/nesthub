这次生效还包括一个底层改动：中文语义匹配从 all-MiniLM-L6-v2 切到了多语模型 paraphrase-multilingual-MiniLM-L12-v2，再加上 label score margin，避免把“4月份一共花了多少钱”这种泛查询误判成餐饮类。若你要继续，我建议下一步做两件事之一：

把这套 semantic label/taxonomy 再扩展到更多消费类别
给 execution_coordinator 补几条针对中文查询的回归测试，避免之后再退化