拆分已经完成了一半：tool handlers 和 registry builder 已经独立成模块，coordinator 体积明显缩小了。现在我做静态检查和回归，确保这次模块拆分没有破坏现有行为。

这一步已经继续推进了，而且方向是对的：不只是少写 if，而是把实现从 coordinator 里真正拆开了。

现在我已经新增了两个模块：
execution_step_handlers.py
这里放通用 tool step 的具体处理逻辑

execution_handler_registry.py
这里负责组装：
_executor_handlers
_step_handlers

然后 execution_coordinator.py 已经改成主要做三件事：
加载 registry
按 executor_type 分发
保留少量编排与少量本地 agent/knowledge 逻辑

这样 coordinator 本体已经比之前更接近“编排器”，而不是“所有逻辑都堆在一起的执行大类”。

回归结果还是稳定的：
11 passed in 15.82s

现在这个结构已经明显更好维护：
新增 tool step 时，不需要再改 coordinator 主分发
registry 和 handler 可以独立演进
后面继续拆 agent handlers / knowledge handlers 时路径已经打通

如果继续往前做，最自然的下一步就是：

把 manage_information_agent 和 query_information_knowledge 这两类 handler 也从 execution_coordinator.py 里拆出去。
再把 registry 做成可扩展注册接口，而不只是当前的静态构建函数。