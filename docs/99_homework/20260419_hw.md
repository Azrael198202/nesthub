Policy 已有完整多模型清單（早已配置），實際缺失的是 step handler 的執行邏輯。現在補全了：

能力	優先鏈
OCR	PaddleOCR → Qwen2.5-VL（Ollama）→ TrOCR（HuggingFace）→ EasyOCR
STT	Whisper-small（本地）→ HuggingFace ASR
TTS	pyttsx3（本地）→ HuggingFace TTS
視頻生成	AnimateDiff（priority 1）→ Stable Video Diffusion（priority 2）
圖片生成	已有：SDXL → SD1.5 → FLUX.1 Schnell → pillow
文檔分析	已有：DeepSeek-R1 + Qwen2.5（Ollama）+ PyMuPDF/Unstructured
網絡查詢	已有：Playwright → Readability → Qwen/DeepSeek 總結
內容過濾	已有：Qwen2.5-3B（Ollama）→ DeepSeek
所有能力都是：先試本地 → 失敗則觸發 CapabilityAcquisitionService 自動安裝（Ollama pull 或 pip install 或 HuggingFace 下載）。


需要給 OCR/STT/TTS/Video 也加外部 API 兜底嗎？比如：

OCR → OpenAI Vision（gpt-4o）
STT → OpenAI Whisper API（whisper-1）
TTS → OpenAI TTS API（tts-1）
視頻 → Replicate（AnimateDiff API）



问题	原因	修复
intent_mismatch 导致直接返回，从不生成图片	image_verification.image_intent_name 未配置，默认用了 "image_generation_task"，但实际 task intent 是 "image_generation"	semantic_policy.json 加 "image_intent_name": "image_generation"
生成的是 pillow 占位文字图	_BACKENDS 顺序把 pillow 放在 openai_api 前面，pillow 永远"成功"	顺序改为 local_diffusion → huggingface_auto → openai_api → pillow
SDXL/SD1.5/FLUX 本地不可用	diffusers 未安装	正常现象，链自动跳到 openai_api，DALL-E 3 返回真实图片 1.7MB


LiteLLM completion() model= qwen2.5:7b-instruct; provider = ollama

Give Feedback / Get Help: https://github.com/BerriAI/litellm/issues/new
LiteLLM.Info: If you need to debug this error, use `litellm._turn_on_debug()'.


# problem 1
LiteLLM completion() model= qwen2.5:7b-instruct; provider = ollama
2026-04-19 19:44:34,072 [INFO] LiteLLM - 
LiteLLM completion() model= qwen2.5:7b-instruct; provider = ollama

Give Feedback / Get Help: https://github.com/BerriAI/litellm/issues/new
LiteLLM.Info: If you need to debug this error, use `litellm._turn_on_debug()'.

2026-04-19 19:44:42,319 [WARNING] nethub_runtime.models - Model invoke retrying (ollama/qwen2.5:7b-instruct) attempt=1 wait=0.50s error=litellm.APIConnectionError: OllamaException - litellm.Timeout: Connection timed out. Timeout passed=8.0, time taken=8.011 seconds

# problem 2