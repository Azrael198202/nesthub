# 2026-04-18 Homework

## Task 1

- 已实现 main session + topic task session 架构：在同一 `session_id` 下引入 `main_session` 与 `task_sessions`。
- 每个 topic 使用独立 task session，避免不同任务在同一个状态槽位中混合。
- 信息型智能体流程在读写状态时优先按 topic 路由，保留 active topic 兼容字段映射。
- 会话上下文窗口已限制为最近 20 条消息，超出自动滚动截断。

## Task 2

- 已补全 Session 与 Vector memory 的持久化逻辑：
  - `SessionStore` 增加文件持久化加载/写回。
  - `VectorStore` 增加向量条目文件持久化加载/写回。
- 持久化实现位于业务域内存层，不改变 core orchestration 对外契约。
- 初始化保持简单：默认自动创建存储目录与文件，支持显式传入路径以便测试与维护。
